{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '⚠️  커밋 전 워크플로우: /be-review → /simplify → /commit → /pr'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,os; d=json.loads(os.environ.get('CLAUDE_TOOL_INPUT','{}')); exit(0 if d.get('file_path','').endswith('.java') else 1)\" && cd /Users/youhyuk/Coming/Backend && ./gradlew compileJava --quiet 2>&1 | grep -E 'error:' | head -20"
          }
        ]
      }
    ]
  }
}
